package hsi.ntest;

public class HEBit {
	public static int most_significant_set_bit(int num){
		int k=1, sbit=-1;
		
		
		return 0;
	}
	
	
	public static void main(String[] args) {
		int iScore=78;
		/*printf("%c \n",iScore>=90 && iScore<=100 ? "A" :   //Java在字串輸出格式無法讀三元運算子
					  iScore>=80 && iScore<=89 ? "B" :
					  iScore>=70 && iScore<=79 ? "C" : "D");
		*/
	}

}
